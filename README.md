# Portfolio

Software engineering portfolio built with Next.js (App Router), React, and
TypeScript. Statically generated, styled with plain CSS / CSS Modules.

## Getting started

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## Scripts

- `npm run dev` — start the development server
- `npm run build` — production build
- `npm run start` — serve the production build
- `npm run lint` — ESLint
- `npm run typecheck` — TypeScript, no emit

## Project structure

```
app/                  routes (App Router)
components/            shared UI (header, footer, nav)
content/projects/     long-form project case studies (later slice)
content/engineering/  long-form engineering writeups (later slice)
data/                  structured/typed content and site config
public/diagrams/      diagram assets
public/images/        image assets
public/resume/        resume file(s)
```
# portfolio-
